source 'https://rubygems.org'

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.0.0'

# Use mysql as the database for Active Record
gem 'mysql2'

# Required to show tables
gem "jquery-datatables-rails", "~> 1.11.2"

# Required for paginating results got from the server to the datatable
gem "will_paginate", "~> 3.0.5"

#required fro datatables look
gem 'jquery-ui-rails'

#required for fileuploads
gem 'jquery-fileupload-rails'

#For forms
gem "simple_form", "~> 3.0.0"

#For fie uploading
gem "paperclip", "~> 3.5.2"

#For asynchronous csv parsing
gem 'delayed_job_active_record'

#For use with delayed job for productions environments
gem "daemons", "~> 1.1.9"


 # See https://github.com/sstephenson/execjs#readme for more supported runtimes
	gem "therubyracer", :require => 'v8'
	
    gem "less-rails", "~> 2.4.2"


	# Use SCSS for stylesheets
	gem 'sass-rails', '~> 4.0.0'

	# Use CoffeeScript for .js.coffee assets and views
	gem 'coffee-rails', '~> 4.0.0'

	

	# Use Uglifier as compressor for JavaScript assets
	gem 'uglifier', '>= 1.3.0'

	gem "twitter-bootstrap-rails", "~> 2.2.8"



# Use jquery as the JavaScript library
gem 'jquery-rails'

# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
gem 'turbolinks'

# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
# gem 'jbuilder', '~> 1.2'

group :doc do
  # bundle exec rake doc:rails generates the API under doc/api.
  gem 'sdoc', require: false
end

# Use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.0.0'

# Use unicorn as the app server
# gem 'unicorn'

# Use Capistrano for deployment
# gem 'capistrano', group: :development

# Use debugger
# gem 'debugger', group: [:development, :test]

gem 'pry'

gem "d3-rails"

# Use Annotate gem for development only
# https://github.com/ctran/annotate_models
group :development do
	gem 'annotate', ">=2.5.0"
	#For debugging use
	gem 'better_errors'
	gem 'binding_of_caller'
	gem 'meta_request'
	#gem "capistrano"
	#commands
	# annotate
	# annotate --routes
end

